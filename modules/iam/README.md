# IAM module

Creates the EC2 application role and instance profile. Permissions are limited to Session Manager, the application bucket, the RDS-managed secret, the project KMS key, and application telemetry.

